<div class="row">
	<div class = "main-header "> <%= raw @h1.article%></div>
	<% @cars.each do |car|%>
		<div class="col-md-4 ">
			<div class="thumbnail product-wrap" >
				<div class="small-img-wrap">
					<a  href=<%=car.url%> title= <%=car.name%> >
						<img src=<%=car.small_photo%> alt=<%=car.name%> > 
					</a>
				</div>
				<div class="caption">
					<h4 class="">
						<a  href=<%=car.url%> title= ""><%=car.short_name%> </a>
					</h4>
					<div class="product-text "><a href = <%=car.url%>  class=" btn btn-primary"  value ="book">price and info </a></div>
				</div>
			</div>
		</div>
	<%end%>
</div>
<br><br>
<%= render 'static_pages/pluses_block' %>

<%= render 'static_pages/contacts' %>
